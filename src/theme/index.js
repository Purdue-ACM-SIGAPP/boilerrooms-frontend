import colors from "./colors";
import { fontSizes, fontWeights, textStyles } from "./typography";
import { spacing, radii, shadows, sizes } from "./spacing";

const theme = { colors, fontSizes, fontWeights, textStyles, spacing, radii, shadows, sizes };

export { colors, fontSizes, fontWeights, textStyles, spacing, radii, shadows, sizes };
export default theme;
